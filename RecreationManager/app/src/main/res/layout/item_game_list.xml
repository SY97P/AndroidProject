<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="itemGameList"
            type="com.example.recreationmanager.GameData" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/game_item"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/recent_destination_item_line_left"
        app:layout_constraintRight_toLeftOf="@+id/recent_destination_item_line_right"
        android:layout_margin="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintDimensionRatio="H,0.99:1">
        <TextView
            android:id="@+id/gameName"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_horizontal"
            android:maxLines="3"
            android:ellipsize="end"
            android:autoSizeTextType="uniform"
            android:autoSizeMaxTextSize="16dp"
            android:autoSizeMinTextSize="14dp"
            android:textColor="#b4b4b4"
            android:textStyle="normal"
            android:textSize="15dp"
            android:text="" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>